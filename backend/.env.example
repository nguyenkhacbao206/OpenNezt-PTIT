# Copy to .env and fill in as needed. All values are optional for mock mode.
# NEVER commit your real .env.

# --- Server ---
HOST=0.0.0.0
PORT=8000

# Default pipeline mode: mock | cloud | offline
DEFAULT_MODE=mock

# CORS (comma-separated). "*" is fine for local dev.
CORS_ORIGINS=*

# --- Cloud STT (leave blank to fall back to mock) ---
STT_API_KEY=
STT_API_URL=

# --- Cloud NMT ---
NMT_API_KEY=
NMT_API_URL=

# --- Cloud TTS ---
TTS_API_KEY=
TTS_API_URL=
