# Copy to .env and fill in as needed. All values are optional for mock mode.
# NEVER commit your real .env.

# --- Server ---
HOST=0.0.0.0
PORT=8000

# Default pipeline mode: mock | cloud | offline
DEFAULT_MODE=mock

# CORS (comma-separated). "*" is fine for local dev.
CORS_ORIGINS=*

# --- Offline STT engine (mode=offline) ---
# whisper -> Faster-Whisper (multilingual, auto-detect)
# sherpa  -> sherpa-onnx per-language: gipformer (VI) + zipformer-en (EN)
STT_ENGINE=whisper
# sherpa-only settings (ignored when STT_ENGINE=whisper):
SHERPA_MODELS_DIR=models
SHERPA_USE_INT8=false
SHERPA_NUM_THREADS=2
SHERPA_DECODING_METHOD=greedy_search

# --- Cloud vendor for mode=cloud: groq (default) | gemini ------------
CLOUD_PROVIDER=groq

# --- Groq (RECOMMENDED free tier) — STT + bidirectional NMT ----------
# Get a free key at https://console.groq.com/keys (starts with gsk_...).
# One key powers both transcription (Whisper) and translation (Llama).
GROQ_API_KEY=
GROQ_API_URL=https://api.groq.com/openai/v1
GROQ_STT_MODEL=whisper-large-v3
GROQ_NMT_MODEL=llama-3.3-70b-versatile
# Optional: split rate limits with a separate key per stage (2 different
# Groq projects/accounts). Leave blank to reuse GROQ_API_KEY.
GROQ_STT_API_KEY=
GROQ_NMT_API_KEY=

# --- Gemini for cloud STT + NMT (CLOUD_PROVIDER=gemini) --------------
# Get a free key at https://aistudio.google.com/apikey and paste the SAME
# key into BOTH STT_API_KEY and NMT_API_KEY. Leave blank to fall back to mock.
GEMINI_MODEL=gemini-2.0-flash

# --- Cloud STT (Gemini: put your Google AI Studio key here) ---
STT_API_KEY=
STT_API_URL=

# --- Cloud NMT (Gemini: put the SAME Google AI Studio key here) ---
NMT_API_KEY=
NMT_API_URL=

# --- TTS (Piper local voices, decoupled from mode) ---
# TTS_ENGINE: piper (real local voices) | mock (silent placeholder clip).
# Works in BOTH cloud and offline mode. Fetch voices first:
#   python tools/download_piper_models.py
TTS_ENGINE=piper
PIPER_MODELS_DIR=models/tts
# Speaking rate: >1.0 slower, <1.0 faster.
PIPER_LENGTH_SCALE=1.0

# --- Cloud TTS (unused by Piper; kept for a future cloud-TTS vendor) ---
TTS_API_KEY=
TTS_API_URL=
